# Local brand-verification data

Only `example_manifest.json` is tracked, and all of its values are synthetic.

Place licensed images under `images/`, or configure another image root with
`BRAND_VERIFICATION_IMAGE_DIR`. Generated manifests and all session outputs
under `annotations/` are local research records and must remain outside Git.
